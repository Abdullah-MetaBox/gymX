node_modules/
dist/
.next/
out/
build/

.env
.env.local
.env.*.local
!.env.example

*.log
npm-debug.log*
pnpm-debug.log*

coverage/
.vitest/

.DS_Store
Thumbs.db

.turbo/
*.tsbuildinfo

# Uploaded files in local dev.
# Anchored to the repo root: an unanchored `storage/` also matches
# packages/storage, which silently kept that whole workspace package out of git.
# Every Vercel build failed with "Cannot resolve '@gymx/storage'" while local
# builds passed, because the package existed on disk but was never committed.
/storage/

.vercel
.env*
# .env.example documents every variable the app needs and must stay in git.
# It survives today only because it was committed before `vercel link` widened
# the pattern above; the negation makes that explicit rather than incidental.
!.env.example
